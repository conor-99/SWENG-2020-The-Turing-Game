# The Turing Game: Dummy Server

This PHP server returns dummy JSON data in response to API requests. 

## Usage:

Navigate to this directory and execute the following command:

`php -S localhost:1234`
